/**
 * Anillo 4 - Consola web (React + Vite + Tailwind + shadcn/ui sobre Radix).
 * Se implementa en Fase 5: navegacion completa por teclado, aria-live para
 * estados asincronos, ausencia de dangerouslySetInnerHTML, WCAG 2.1 AA.
 *
 * No inicia hasta que el anillo 3 este cerrado, probado y auditado.
 */
export const RING = "A4-web" as const;
