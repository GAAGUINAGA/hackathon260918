/**
 * Anillo 1 (Aplicacion). Orquesta casos de uso contra puertos.
 * Prohibido importar infraestructura real (ADR-09, regla application-no-infra).
 *
 * Se implementa en Fase 2: ActorContext obligatorio con owner_id (RT-01),
 * puertos de salida (ContactRepositoryPort, ContactProviderPort,
 * ContactQueryPort bajo ADR-19a) y puertos diferidos (LlmPort, EmbeddingPort).
 */
export {};
