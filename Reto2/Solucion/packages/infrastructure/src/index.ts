/**
 * Anillo 2 (Infraestructura). Implementa los puertos de @ssot/application
 * contra Postgres/Drizzle, Redis/BullMQ, cripto y adaptadores externos.
 *
 * Se implementa en Fase 3: migraciones Drizzle (16 tablas), RLS FORCE +
 * WITH CHECK, SET LOCAL app.current_user_id, cifrado AES-256-GCM,
 * Outbox transaccional con rele LISTEN/NOTIFY + sondeo adaptativo.
 */
export {};
