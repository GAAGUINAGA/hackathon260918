# Vite carga este archivo automáticamente en modo "test" (vitest). Valores
# ficticios: los tests interceptan la red con MSW, nunca llaman a Supabase
# ni a apps/api de verdad.
VITE_SUPABASE_URL=https://test-project.supabase.co
VITE_SUPABASE_ANON_KEY=test-anon-key
VITE_API_BASE_URL=http://localhost:3000
